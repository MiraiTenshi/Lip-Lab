node_modules
dist
.DS_Store
*.local

# Generated product images/manifest are build output — regenerated by
# `npm run manifest` / the GitHub Action. Keep the source photos in
# /products (tracked), not the derived copies here.
public/products/*
!public/products/.gitkeep
